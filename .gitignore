# Build artifacts
/build/
/openbench-build/
/bin/
*.o
*.a
*.so
*.exe
*.out
*.app

# macOS build directories
/build-macos/
/build-macos-cross/
/bin-macos/

# External tools and engines (downloaded separately)
/external/

# Large NNUE files (except default)
*.nnue
!networks/default.nnue

# IDE and editor files
.vscode/          # Local VSCode settings (workspace settings are OK to commit)
.idea/
*.swp
*.swo
*~
.DS_Store
.cache/           # clangd cache

# Debug files
*.dSYM/
*.pdb
*.ilk

# Profiling
*.gcda
*.gcno
*.gcov
gmon.out
callgrind.out.*
perf.data
*.profdata        # Profile-guided optimization
*.profraw

# Local configuration
.env.local
*.local

# AI assistant context files (should never be tracked)
CLAUDE.md         # AI context file (may contain sensitive information)
.claude/          # Claude-specific configuration directory
container-config.sh  # Container configuration script
yolo.sh           # Claude permission bypass script
config.json       # Configuration file
.claude/          # Claude-specific directory
.devcontainer/    # Dev container configuration
feature_status.md # Temporary feature tracking (only on feature branches)

# Test outputs
*.log
*.pgn
*.epd.out

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile.cmake
compile_commands.json

# Dependencies
/deps/
/lib/
node_modules/
package-lock.json

# Temporary files
*.tmp
*.temp
/tmp/.devcontainer/devcontainer.json.local

# Machine-specific environment
.env
