# Machine-specific environment configuration
# Copy this to .env and customize for each machine
# DO NOT commit .env to git

# Machine identifier (e.g., "macbook", "studio")
MACHINE_NAME=macbook

# Number of CPU cores to use
NPROC=4

# Git configuration (optional)
GIT_USER_NAME="Your Name"
GIT_USER_EMAIL="your.email@example.com"

# Timezone
TZ=America/Chicago

# Docker resource limits (adjust based on machine)
# For MacBook, you might use less resources
# For Mac Studio, you can use more
DOCKER_CPUS=4
DOCKER_MEMORY=12g